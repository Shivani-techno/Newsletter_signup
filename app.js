const express=require("express");
const https=reuire("https");
const app=require();
const bodyParser=require("body-parser");
const request=require("requesr");




app.listen(3000,function(){
  console.log("Server is running on port 3000.");
});
